name: Pruebas del MVP web

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  django:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DJANGO_DEBUG: "1"
      DJANGO_DB_PATH: /tmp/bitaturno-ci.sqlite3
      DJANGO_MEDIA_ROOT: /tmp/bitaturno-media
      DJANGO_STATIC_ROOT: /tmp/bitaturno-static
      DATA_TRANSFER_WORK_ROOT: /tmp/bitaturno-transferencias

    defaults:
      run:
        working-directory: web

    steps:
      - name: Obtener el repositorio
        uses: actions/checkout@v7

      - name: Configurar Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-lock.txt

      - name: Instalar dependencias verificadas
        run: python -m pip install -r ../requirements-lock.txt

      - name: Aplicar migraciones en SQLite temporal
        run: python manage.py migrate --noinput

      - name: Ejecutar comprobación de Django
        run: python manage.py check

      - name: Ejecutar pruebas automatizadas
        run: python manage.py test

      - name: Validar estructura y navegación del mockup
        run: python ../tests/test_mockup.py

      - name: Comprobar migraciones pendientes
        run: python manage.py makemigrations --check --dry-run
